/**
 * DTOs de entrada (o que o cliente ENVIA). JavaBeans clássicos (setters), o formato que o Jackson
 * espera para desserializar JSON sem anotação extra. Anotados com Bean Validation
 * ({@code javax.validation.constraints}), disparada pelo {@code @Valid} nos controllers.
 */
package br.jus.trt4.processo.dto.request;
