/**
 * DTOs de saída (o que a API DEVOLVE). JavaBeans clássicos (construtor sem argumentos + setters)
 * — é a estratégia padrão do MapStruct para popular um objeto a partir de uma entidade, ver
 * comentário em {@link ParteResponseDTO} sobre a tentativa (fracassada, nesta versão do
 * MapStruct) de usar mapeamento por construtor.
 */
package br.jus.trt4.processo.dto.response;
